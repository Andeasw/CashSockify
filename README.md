# ClashSockify
Convert your Clash YAML configuration file into a ready-to-use local SOCKS5 proxy service configuration. All proxies are protected by user-defined usernames and passwords. Because it runs entirely within your browser, your configuration always remains private and never leaves your computer.
